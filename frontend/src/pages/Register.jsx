import { useNavigate } from 'react-router-dom'

export default function Register() {
  const navigate = useNavigate()
  
  // This component is not used as we handle registration in Login.jsx
  // But keeping it for routing purposes
  return null
}
